(include "../../signature/programs/trees.eo")
(include "../../signature/programs/lists.eo")
(include "../unit_test_services.eo")


; Unit tests for trees.eo programs

;---------------------------------
; $binary_tree_remove_elem
;---------------------------------

(step test true :rule $unit_test :args (($binary_tree_remove_elem + ($f_list_cons + 1 2) 1)

                                        2))

(step test true :rule $unit_test :args (($binary_tree_remove_elem + ($f_list_cons + 1 2) 2)

                                        1))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + 
                                         ($f_list_cons + 1 
                                                       ($f_list_cons + 2 3)) 1)

                                        ($f_list_cons + 2 3)))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + 
                                         ($f_list_cons + 1 ($f_list_cons + 2 3)) 2)

                                        ($f_list_cons + 1 3)))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + ($f_list_cons + 1 ($f_list_cons + 2 3)) 3)

                                        ($f_list_cons + 1 2)))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + 
                                         ($f_list_cons + 1 
                                                       ($f_list_cons 
                                                        + 2 ($f_list_cons + 4 5))) 1)

                                        ($f_list_cons + 2 ($f_list_cons + 4 5))))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + 
                                         ($f_list_cons + 1 
                                                       ($f_list_cons 
                                                        + 2 ($f_list_cons + 4 5))) 2)

                                        ($f_list_cons + 1 ($f_list_cons + 4 5))))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + 
                                         ($f_list_cons 
                                          + 1 
                                          ($f_list_cons + 2 ($f_list_cons + 4 5))) 4)

                                        ($f_list_cons + 1 ($f_list_cons + 2 5))))

(step test true :rule $unit_test :args (($binary_tree_remove_elem 
                                         + 
                                         ($f_list_cons 
                                          + 1 
                                          ($f_list_cons + 2 ($f_list_cons + 4 5))) 5)
                                        
                                        ($f_list_cons + 1 ($f_list_cons + 2 4))))
;---------------------------------
; $binary_tree_get_left_most_leaf
;---------------------------------
; Addition
(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         + ($f_list_cons + 2 1))
                                        
                                        2))

(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         + ($f_list_cons + 
                                                         ($f_list_cons + 1 3)
                                                         2))
                                        
                                        1))

(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         + 
                                         ($f_list_cons +
                                                       ($f_list_cons * 1 3)
                                                       2))

                                        ($f_list_cons * 1 3)))

(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         + 
                                         ($f_list_cons +
                                                       ($f_list_cons + 
                                                                     ($f_list_cons * 1 3)
                                                                     1)
                                                         2))

                                        ($f_list_cons * 1 3)))

; Product
(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         * ($f_list_cons * 2 1))

                                        2))

(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         * ($f_list_cons * 
                                                         ($f_list_cons * 1 3)
                                                         2))

                                        1))

(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         * ($f_list_cons *
                                                         ($f_list_cons + 1 3)
                                                         2))

                                        ($f_list_cons + 1 3)))

(step test true :rule $unit_test :args (($binary_tree_get_left_most_leaf 
                                         * ($f_list_cons *
                                                         ($f_list_cons * 
                                                                       ($f_list_cons + 1 3)
                                                                       1)
                                                         2))

                                        ($f_list_cons + 1 3)))
